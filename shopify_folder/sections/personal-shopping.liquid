{%- style -%}
  /* Shared Info-section CSS */
  .pp-content {
    padding: 96px 150px;
    background: #fff;
    display: flex;
    flex-direction: column;
    gap: 56px;
  }

  .pp-content-block {
    display: flex;
    flex-direction: column;
    gap: 32px;
  }

  .pp-block-title {
    font-family: "Inter", sans-serif;
    font-size: 24px;
    font-weight: 400;
    line-height: 100%;
    letter-spacing: 0;
    color: #2d1f1f;
    margin: 0;
  }

  .pp-block-title--upper {
    font-size: 16px;
    font-weight: 500;
    text-transform: uppercase;
  }

  .pp-block-body {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .pp-block-intro {
    font-family: "Inter", sans-serif;
    font-size: 16px;
    font-weight: 400;
    line-height: 150%;
    color: #1a1a1a;
    margin: 0;
  }

  .pp-block-bullets {
    margin: 0;
    padding-inline-start: 40px;
    list-style: none;
    font-family: "Inter", sans-serif;
    font-size: 16px;
    font-weight: 400;
    line-height: 150%;
    color: #1a1a1a;
  }

  .pp-block-bullets li {
    position: relative;
    margin: 0;
    padding-inline-start: 18px;
  }

  .pp-block-bullets li::before {
    content: "";
    position: absolute;
    inset-inline-start: 0;
    top: 9px;
    width: 6px;
    height: 6px;
    background: #1a1a1a;
    border-radius: 50%;
  }

  .pp-block-bullets li + li {
    margin-top: 2px;
  }

  .pp-block-intro + .pp-block-bullets {
    margin-top: -8px;
  }

  /* Personal Shopping Hero */
  .ps-hero {
      padding: 96px 150px 48px;
      background: #fff;
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 20px;
  }

  .ps-hero-title {
      font-family: 'Inter', sans-serif;
      font-size: 64px;
      font-weight: 400;
      color: #2D1F1F;
      margin: 0;
      line-height: 1.1;
      text-transform: capitalize;
  }

  .ps-hero-subtitle {
      font-family: 'Inter', sans-serif;
      font-size: 16px;
      font-weight: 400;
      color: #666;
      margin: 0;
      line-height: 1.5;
  }

  /* Contact Form Section */
  .contact-form-section {
      background: #fff;
      padding: 96px 150px;
      position: relative;
      width: 100%;
      box-sizing: border-box;
  }

  .contact-form-section::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 1px;
      background: #e5e5e5;
  }

  .contact-form-inner {
      max-width: 1620px;
      margin: 0 auto;
      display: flex;
      align-items: flex-start;
      gap: 150px;
  }

  .contact-form-intro {
      flex: 0 0 488px;
      display: flex;
      flex-direction: column;
      gap: 16px;
  }

  .contact-form-intro h2 {
      font-family: 'Inter', sans-serif;
      font-size: 38px;
      font-weight: 400;
      line-height: 100%;
      margin: 0;
      color: #2D1F1F;
  }

  .contact-form-intro p {
      font-family: 'Inter', sans-serif;
      font-size: 16px;
      color: rgba(45, 31, 31, 0.7);
      margin: 0;
  }

  .contact-form-wrapper {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 12px;
  }

  .contact-form-wrapper form {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
  }

  .contact-form-wrapper form span.full-width {
      grid-column: 1 / -1;
  }

  .contact-form-wrapper input[type="text"],
  .contact-form-wrapper input[type="email"],
  .contact-form-wrapper input[type="tel"],
  .contact-form-wrapper textarea {
      width: 100%;
      background: #fff;
      border: 1px solid rgba(45, 31, 31, 0.2);
      border-radius: 6px;
      color: #2D1F1F;
      font-family: 'Inter', sans-serif;
      font-size: 14px;
      box-sizing: border-box;
      outline: none;
  }

  .contact-form-wrapper input[type="text"],
  .contact-form-wrapper input[type="email"],
  .contact-form-wrapper input[type="tel"] {
      height: 44px;
      padding: 0 12px;
  }

  .contact-form-wrapper textarea {
      height: 108px;
      padding: 12px;
      resize: vertical;
  }

  .contact-submit-custom {
      width: 100%;
      height: 48px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      background: #fff;
      color: #2D1F1F;
      border: 1px solid #2D1F1F;
      border-radius: 900px;
      font-family: 'Inter', sans-serif;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
  }

  .contact-submit-custom:hover {
      background: #2D1F1F;
      color: #fff;
  }

  .contact-bottom-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 20px;
      margin-top: 16px;
  }

  .contact-toggle {
      display: flex;
      gap: 8px;
  }

  .contact-toggle-pill {
      background: #FAF8F5;
      border: 1px solid #E0DCD3;
      padding: 8px 16px;
      border-radius: 30px;
      font-family: 'Inter', sans-serif;
      font-size: 14px;
      cursor: pointer;
      color: #666;
      transition: all 0.2s ease;
  }

  .contact-toggle-pill.active {
      background: #2D1F1F;
      color: #fff;
      border-color: #2D1F1F;
  }

  .wpcf7-response-output {
      grid-column: 1 / -1;
      margin: 12px 0 0;
      padding: 14px 18px;
      font-size: 14px;
      border-radius: 4px;
      color: #2D1F1F;
      border: 1px solid rgba(45, 31, 31, 0.2);
  }

  .wpcf7-response-output.sent {
      border-color: #28a745;
      color: #1e7a35;
      background-color: rgba(40, 167, 69, 0.05);
  }

  .wpcf7-response-output.invalid {
      border-color: #d93b3d;
      color: #b1282a;
      background-color: rgba(217, 59, 61, 0.05);
  }

  @media (max-width: 1023px) {
      .ps-hero,
      .pp-content,
      .contact-form-section {
          padding: 64px 40px;
      }
      .contact-form-inner {
          flex-direction: column;
          gap: 40px;
      }
  }

  @media (max-width: 767px) {
      .ps-hero,
      .pp-content,
      .contact-form-section {
          padding: 48px 24px;
      }
      .ps-hero-title {
          font-size: 32px;
      }
      .contact-form-wrapper form {
          grid-template-columns: 1fr;
      }
      .contact-bottom-row {
          flex-direction: column;
          align-items: stretch;
      }
      .contact-toggle {
          justify-content: center;
      }
  }
{%- endstyle -%}

<script>
  document.body.classList.add('personal-shopping-page-body');
</script>

<main class="ps-main">
    <!-- Hero Section -->
    <header class="ps-hero">
        <h1 class="ps-hero-title">{{ section.settings.heading | default: "Personal Shopping" }}</h1>
        <p class="ps-hero-subtitle">{{ section.settings.subheading | default: "Access products sourced specifically on demand for you." }}</p>
    </header>

    <!-- Stacked Content Blocks (Text First) -->
    <section class="pp-content">
        {%- for block in section.blocks -%}
            {%- if block.type == 'content_block' -%}
                <div class="pp-content-block" {{ block.shopify_attributes }}>
                    {%- if block.settings.title != blank -%}
                        <h2 class="pp-block-title{% if block.settings.uppercase %} pp-block-title--upper{% endif %}">
                            {{ block.settings.title }}
                        </h2>
                    {%- endif -%}
                    
                    {%- if block.settings.intro != blank or block.settings.bullets != blank -%}
                        <div class="pp-block-body">
                            {%- if block.settings.intro != blank -%}
                                <p class="pp-block-intro">{{ block.settings.intro | newline_to_br }}</p>
                            {%- endif -%}
                            
                            {%- if block.settings.bullets != blank -%}
                                <ul class="pp-block-bullets">
                                    {%- assign bullet_items = block.settings.bullets | split: '||' -%}
                                    {%- for item in bullet_items -%}
                                        {%- if item != blank -%}
                                            <li>{{ item | strip }}</li>
                                        {%- endif -%}
                                    {%- endfor -%}
                                </ul>
                            {%- endif -%}
                        </div>
                    {%- endif -%}
                </div>
            {%- endif -%}
        {%- endfor -%}
    </section>

    <!-- Contact Form Section -->
    <section class="contact-form-section">
        <div class="contact-form-inner">
            <div class="contact-form-intro">
                <h2>{{ section.settings.form_heading | default: "Send Us A Message" }}</h2>
                <p>{{ section.settings.form_subheading | default: "Fill out the form and we'll get back to you as quickly as possible." }}</p>
            </div>

            <div class="contact-form-wrapper">
                {%- form 'contact', id: 'ContactForm' -%}
                    {%- if form.posted_successfully? -%}
                        <div class="wpcf7-response-output sent" role="status">
                            Thanks for contacting us! We'll get back to you as soon as possible.
                        </div>
                    {%- elsif form.errors -%}
                        <div class="wpcf7-response-output invalid" role="alert">
                            {{ form.errors | default_errors }}
                        </div>
                    {%- endif -%}

                    <p>
                        <span>
                            <input type="text" name="contact[first_name]" placeholder="First Name" value="{% if form.first_name %}{{ form.first_name }}{% elsif customer %}{{ customer.first_name }}{% endif %}" required aria-required="true" />
                        </span>
                        <span>
                            <input type="text" name="contact[last_name]" placeholder="Last Name" value="{% if form.last_name %}{{ form.last_name }}{% elsif customer %}{{ customer.last_name }}{% endif %}" required aria-required="true" />
                        </span>
                        <span>
                            <input type="email" name="contact[email]" placeholder="Email" value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}" required aria-required="true" />
                        </span>
                        <span>
                            <input type="tel" name="contact[phone]" placeholder="Phone Number (optional)" value="{% if form.phone %}{{ form.phone }}{% elsif customer %}{{ customer.phone }}{% endif %}" />
                        </span>
                        <span class="full-width">
                            <textarea cols="40" rows="10" name="contact[body]" placeholder="Your Message" required aria-required="true">{% if form.body %}{{ form.body }}{% endif %}</textarea>
                        </span>
                        <input type="hidden" name="contact[customer_type]" value="qatar" id="contact-customer-type" />
                        <input type="submit" class="wpcf7-submit" id="RealContactSubmit" value="Send Message" style="display:none;" />
                    </p>
                {%- endform -%}

                <div class="contact-bottom-row">
                    <div class="contact-toggle">
                        <button type="button" class="contact-toggle-pill active" data-value="qatar">Qatar Customer</button>
                        <button type="button" class="contact-toggle-pill" data-value="international">International Customer</button>
                    </div>
                    <button type="button" class="contact-submit-custom" onclick="document.getElementById('RealContactSubmit').click();">
                        <svg class="contact-submit-icon" width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                            <path d="M17.5 2.5L9.16667 10.8333" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                            <path d="M17.5 2.5L12.0833 17.5L9.16667 10.8333L2.5 7.91667L17.5 2.5Z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Send a Message</span>
                    </button>
                </div>
            </div>
        </div>
    </section>
</main>

<script>
  (function() {
      // Toggle pills for customer type
      var pills = document.querySelectorAll('.contact-toggle-pill');
      pills.forEach(function(pill) {
          pill.addEventListener('click', function() {
              pills.forEach(function(p) { p.classList.remove('active'); });
              this.classList.add('active');
              var hidden = document.getElementById('contact-customer-type');
              if (hidden) hidden.value = this.getAttribute('data-value');
          });
      });
  })();
</script>

{% schema %}
{
  "name": "Personal Shopping",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Personal Shopping"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Access products sourced specifically on demand for you."
    },
    {
      "type": "header",
      "content": "Contact Form Settings"
    },
    {
      "type": "text",
      "id": "form_heading",
      "label": "Form Heading",
      "default": "Send Us A Message"
    },
    {
      "type": "text",
      "id": "form_subheading",
      "label": "Form Subheading",
      "default": "Fill out the form and we'll get back to you as quickly as possible."
    }
  ],
  "blocks": [
    {
      "type": "content_block",
      "name": "Content Block",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Block Title",
          "default": "HOW IT WORKS"
        },
        {
          "type": "checkbox",
          "id": "uppercase",
          "label": "Uppercase Heading",
          "default": true
        },
        {
          "type": "textarea",
          "id": "intro",
          "label": "Intro Paragraph"
        },
        {
          "type": "textarea",
          "id": "bullets",
          "label": "Bullet Items (separated by ||)",
          "default": "Get in touch with our customer care team via WhatsApp and share the product details — style, size, and any preferences||Our team will confirm availability and provide full details before any payment is taken.||Once confirmed, payment is collected in full via Bank Transfer before sourcing begins.||Your item is sourced exclusively for you and dispatched within the confirmed timeframe."
        }
      ]
    }
  ]
}
{% endschema %}
